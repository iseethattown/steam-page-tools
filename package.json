{
  "name": "steam-page-tools",
  "version": "1.2.0",
  "private": true,
  "description": "An unofficial Chrome and Firefox extension with Steam profile links, badge tools, friends-page comments, and bulk Store tools.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run clean && npm run lint && npm run validate && npm run verify:reproducible && npm run validate",
    "clean": "node scripts/build.mjs --clean",
    "lint": "eslint .",
    "package": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "verify:reproducible": "node scripts/verify-reproducible.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@11.13.0",
  "devDependencies": {
    "eslint": "10.6.0"
  }
}
